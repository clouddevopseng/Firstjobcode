node {
    stage('Download git code from github Everytime') {
    git branch: 'qa', url: 'https://github.com/clouddevopseng/Firstjobcode.git'
     }
     stage('Convert into artifacts') {
    sh 'mvn package'
     }
     stage('Deploy') {
     deploy adapters: [tomcat9(credentialsId: 'qa', path: '', url: 'http://172.31.11.138:8080')], contextPath: '/qa-new-17-04', war: '**/*.war'
     }
}
